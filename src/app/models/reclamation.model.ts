export interface Reclamation {
    id?: number; // Optionnel
  idreclamation?: number;
  titre: string;
  description: string;
  type: string;
  dateReclamation?: string;
  status?: string;
  user?: any;
}
